/*global define:true*/

'use strict';

define('Collider',

//['Dependency'],

function (/*Dependency*/)
{
    /*
    Collider Class
    Collider Description
    */
    return function ()
    {
        // PRIVATE part of the class

        // PUBLIC part of the class
        var that =
            {
                // Define the component as Active or not
                active: true,

                // Init the Collider
                Start: function ()
                {

                },

                // Update the Collider
                Update: function ()
                {

                },

                // Update the Collider, at fixed interval
                FixedUpdate: function ()
                {

                },

                // Update the Collider, after all update call
                LateUpdate: function ()
                {

                },

                // Render the Collider
                Render: function ()
                {

                }
            };

        return that;
    };
});