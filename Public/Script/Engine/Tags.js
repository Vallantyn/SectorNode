/*global define:true*/

'use strict';

define('Tags', function ()
{
    /*
    Global Object holding a reference to the differents tags sets;
    */
    var TAGS =
        [
            'none' // Default tag
        ];

    return TAGS;
});